<x-layout>
    <section class="hero">
        <div class="container">
            <h1>Dashboard</h1>
        </div>
        <a href="{{route('categories.create')}}" class="btn-create">+ Add Category</a>
        <a href="{{route('categories.index')}}" class="btn-create">View Categories</a>
    </section>
</x-layout>